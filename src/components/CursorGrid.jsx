// src/components/CursorGrid.jsx
import React from "react";

export default function CursorGrid() {
  return null; // ✅ We no longer need a <div> — all effects are handled by body::before and body::after
}
